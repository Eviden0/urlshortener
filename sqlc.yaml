version: "2"
sql:
  - engine: "postgresql"
    queries: "db/query"
    schema: "db/migration"
    gen:
      go:
        package: "repo"
        out: "internal/repo"
        emit_json_tags: true
        emit_interface: true
        
